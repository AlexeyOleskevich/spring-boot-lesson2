spring:
  application:
      issue.max-allowed-books = 2
  datasource:
    url: jdbc:h2:mem:test
    username: kk
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update



